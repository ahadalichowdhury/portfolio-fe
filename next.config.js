/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'plus.unsplash.com',
      'images.unsplash.com',
      'unsplash.com',
      'media.licdn.com'
    ],
  },
}

module.exports = nextConfig
